# Telegram 机器人 Token（从 @BotFather 获取）
TELEGRAM_BOT_TOKEN=your-bot-token-here

# 服务端口（默认 3007）
PORT=3007

# 可选：指定 WezTerm 窗格 ID（不设置则自动检测）
# WEZTERM_PANE_ID=0

# 可选：Bridge URL（用于 hook 脚本，默认 http://localhost:3007/hook）
# BRIDGE_URL=http://localhost:3007/hook
